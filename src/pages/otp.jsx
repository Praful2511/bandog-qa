import React from "react";

import { OtpCard } from "../components/otpCard";
export const OTP = (params) => {
  return (
    <div>
      <OtpCard buttonText={ "Verify OTP & Sign Up"} />
    </div>
  );
};
